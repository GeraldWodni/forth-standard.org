//- (c)2015 by Gerald Wodni

extends layout

//block menu
//    +menu

block content
    +box
        h1= wordSet.name
        ul.row
            each word in wordSet.words
                li.col-xs-4
                    a(href="/standard/#{wordSet.name}/#{word.basename}")
                        = word.name
